<!-- Find Max Value Algorithm -->
STORE variable number1
STORE variable number2
STORE variable number3
STORE variable maxtemp
DECLARE variable max

IF number1 greater than number2 then
maxtemp = number1
else
maxtemp = number2

ELSE
IF number3 greater than maxtemp then
maxtemp = number3

DISPLAY max=maxtemp
<!-- ------------------------ -->
